package com.egabi.university.entity.authentication;

/**
 * Enumeration representing different user roles in the system.
 * Each role corresponds to a specific type of user with distinct permissions.
 */
public enum Role {
    STUDENT,
    INSTRUCTOR,
    ADMIN
}
